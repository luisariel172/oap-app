const Titulo = ({ titulo, text }) => {
    return <h1 style={{ color : 'violet' }}>{titulo} {text}</h1>    
}
//Aqui se arma el componente, las props se les pasan y el componente queda armado
export default Titulo